public class Tricks {

}

// type of int
// for (Integer ele : al) {
// int primitive = ele.intValue(); // or just int primitive = ele;
// (auto-unboxing)
// System.out.println("Primitive Type: int, Value: " + primitive);
// }

// data type of objects
// System.out.println(ele.intValue().getClass().getSimpleName());

// Arrays.asList(1, 2, 3)

// al.toArray(new Integer[0])
// set.toArray()

// Set<String> fruitSets = new HashSet<>(Arrays.asList(fruitss));

// Collections.addAll(fruitSets,fruitArray)